class node:
	def __init__(self, val, height=1):
		self.__val = val
		self.__height = height
		self.__left = None
		self.__right = None

class AVLtree:
	def __init__(self):
		self.__root = None

	def insert(self, val):
		def searchPositionAndInsert(self, node)
			

		if self.__root is None:
			self.__root = node(val)
		else:
